package main

import "github.com/spf13/cobra-cli/cmd"

func main() {
	// InitUser("Ilian")
	/*newTask("Test", 3, "This is a test to do sutff with a task tracer")
	newTask("lala", 5, "This is a test to do sutff with a task tracer")
	newTask("test", 4, "This is a test to do sutff with a task tracer")
	newTask("hello", 123, "This is a test to do sutff with a task tracer")
	newTask("why", 8, "This is a test to do sutff with a task tracer")
	newTask("Erfolg", 8, "This is a test to do sutff with a task tracer")
	completeTask(1)
	completeTask(3)
	completeTask(4)
	InitUser("") */
	/*printTasks()
	storeArray(currentUser.List, currentUser.Name)
	currentUser.List = readArray(currentUser.Name)
	printTasks() */
	cmd.Execute()
}
